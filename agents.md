# Agent Instructions

- Read this file before touching the repo.
- Always run `make lint` and `make test` after making a change.
- Always commit and push after every completed change, even when working on `main`.
- Keep changes small enough that each commit has a clear purpose.
- Use `make build` before `make deploy`.
- Preserve the `v1.0` tag as the frozen legacy baseline.
